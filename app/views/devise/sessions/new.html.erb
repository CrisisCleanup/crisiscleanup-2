<% content_for :title do %>
    Crisis Cleanup - Login
<% end %>

<% content_for :main do %>
<h2>Log in</h2>
<p>Welcome to the new Crisis Cleanup. You now <u>CHOOSE</u> your own password. If you don't have a password:</p>
<p>
	<ul>
		<li><a href="/register">Register Your Org</a> for the first time.</li>
		<li><a href="/volunteer">Volunteer</a> by requesting access from a fellow volunteer.</li>
	</ul>
<p>
<p>Click "Help" if you are the primary contact for your organization, and do not have access to it. Be sure to include the name of your organization.</p>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, required: false, autofocus: true %>
    <%= f.input :password, required: false %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Log in" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
<br>
<%end%>