<table>
  <thead>
    <tr>
      <th width="200">Case Label</th>
      <th>Name</th>
      <th width="150">Start Date</th>
      <th width="150">End Date</th>
    </tr>
  </thead>
  <tbody>
    <% events.each do |event| %>
      <tr>
        <td>
          <a href=<%= admin_stats_by_incident_path(event.id)%>> 
            <%= event.case_label %>
          </a>
        </td>
        <td> <%= event.name %> </td>
        <td> <%= event.start_date %> </td>
        <td> <%= event.end_date %> </td>
      </tr>
    <% end %>
  </tbody>
</table>