module ApplicationHelper
    def check_admin?
        # here is a method to check if we are logged in
        # if not...redirect
    end
    def is_super_admin?
        # current_user.is_ccu_admin?
    end
end
