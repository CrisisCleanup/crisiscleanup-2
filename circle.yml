machine:
  ruby:
    version: ruby-2.2.1

dependencies:
  post:
    - bundle exec rake assets:precompile

test:
  override:
    - bundle exec rspec --color --require spec_helper --format RspecJunitFormatter --out /tmp/circle-junit.dDz3fWr/rspec/rspec.xml spec --format progress